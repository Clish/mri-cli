#!/usr/bin/env node

/**
 *  mri dev ${theme}
 *  启动 MRI 项目
 */

'use strict';

const $root = require('../service/root');
const $_project = require('../process/project');
const MRI_ENV = 'dev';

$root.run(() => {
    $_project(MRI_ENV);
});
