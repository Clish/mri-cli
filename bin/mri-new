#!/usr/bin/env node

'use strict';

const _program    = require('commander');

_program
    .usage('name [key1:value1 key2:value2...]')
    .parse(process.argv);


console.log(`\n该命令已改名，请使用 mri theme\n`);


// process.stdin.resume();
// process.stdin.setEncoding("utf8");
// process.stdin.on("data", function(chunk) {
//
//     let _s = + new Date();
//
//     let [n, start, step] = chunk.split(' ');
//     n = + n;
//     start = + start - 1;
//     step = + step - 1;
//
//     let cols = [];
//     for(let i = 0; i < n; i++ ){
//         cols[i] = i + 1;
//     }
//
//     // while(cols.length > 1) {
//     //     start = (start + step) % cols.length;
//     //     cols.splice(start, 1);
//     // }
//
//     let length = cols.length;
//
//     for(let i = 0; i < length - 1; i ++ ){
//         start = (start + step) % cols.length;
//         cols.splice(start, 1);
//     }
//
//     console.log(cols[0],  + new Date() - _s);
// });
