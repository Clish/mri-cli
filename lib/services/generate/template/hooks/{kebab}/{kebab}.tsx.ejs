/**
 * <%= className %>
 * 创建React Hook
 * @create <%= date %>
 */

import { useState } from 'react';
import mu from 'mzmu';
import * as _ from 'lodash';
import './<%=kebab%>.less';

<% if (config.props) {%>
interface <%=className%>Props {}
function <%= className %> (props: <%=className%>Props) {
}
<% } else { %>
function <%= className %> () {
}
<% } %>

export default <%= className %>;

