/**
 * umi 以及 mri-cli 的相关配置
 *
 * umi -> https://umijs.org/config/#%E5%9F%BA%E6%9C%AC%E9%85%8D%E7%BD%AE
 */

const path = require('path');
const resolve = path.resolve;
const themeName = process.env.THEME;

module.exports = {

    /**
     * 接口服务代理
     */
    proxy: {
        '/services': {
            target: 'http://58.215.174.164:16800/',
            changeOrigin: true,
            pathRewrite: {'^/services': ''}
        }
    },

    /**
     * 主题颜色配置
     */
    theme: {
        'primary-color': '<%=primaryColor%>',
    },

    /**
     * mri-cli 配置
     */
    mri: {
        // 当前主题
        PROJECT: '<%=name%>',

        // 项目运行设备
        MRI_DEVICE: 'pc',

        // dev 环境， web-server 启动端口
        PORT: '<%=port%>'

        // layout 组成方式
        /**
         * Layout 产生的数据，是否可以直接向下传递数据（与Context静态数据不同，动态传递数值)
         * - ligation (默认值) 非传递模式，数据不向下传递; 其表现为页面route改变时候，layout区域不重新渲染
         * - transmit 传递模式，数据向下第一层传递数（与Context不同，不会逐层传递）；其表现为，route改变, layout重新渲染
         */
        LAYOUT: 'ligation'
    }
};
