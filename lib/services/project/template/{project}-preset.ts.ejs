/**
 * preset: project 预设置
 * 替换原先将 预设置防止在 dva 中不合理的方案
 *
 * preset 通常放置 MasterRT 预设置信息
 * 以及第三方插件预设置
 */

import { MrServices } from 'masterrt';
import $project from 'src/project';
import mu from 'mzmu';

export default (module) => {

};
