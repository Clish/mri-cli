/**
 * <%=title ? 'title: ' + title : ''%>
 */
import * as React from 'react';
import $project from '@/project';
const module = $project.getModule('<%=name%>');
<% if (redirect) { %>
import Redirect from 'umi/redirect';
export default () => <Redirect to={'<%=redirect%>'} />
<% } else { %>
import { MrComponent } from 'masterrt';
import $jsx from '@/services/jsx.services';
const module = $project.getModule('dashboard');
const component = module.component;

export default () => {
  return (<MrComponent component={$project.getLayout()}>
      {$jsx.recursion($project.getSubLayout(), component)}
    </MrComponent>);
};
<% } %>

