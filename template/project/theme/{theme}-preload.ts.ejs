/**
 * theme 预加载数据
 * 返回 Promise
 */

import { MrResource } from 'masterrt';
import $pool from 'src/services/base.resources';
import StoreServices from 'src/common/services/store.services';

export default (module) => {
    if (!module) {
        return void 0;
    }

    // let $$test: any;

    if (module.token) {
    } else {
        // exp.
        // let test = MrResource.pool('/services/demo')
        // $$test = test.get({}).then((rst) => {
        //      StoreServices.setValue('test', rst);
        // })
    }

    return Promise.all([
        // $$test
    ]);
};
